---
name: tone-fix
description: 한국어 문서, MD, 인포그래픽, 강의자료, 슬라이드 덱의 AI 말투, 번역체, 구조, 가독성을 3단계(1 패턴 스캔 → 2 문장, 어휘 + 문단, 구조 교정 → 3 키프레임, 문단, 줄바꿈)로 교정할 때 사용. 제목/부제의 서사형, 수사의문문, 상투어(한눈에, 살펴보기, 정리하면, ~란?) 제거, 1인칭, 방어 톤, 가정형, 추측 단어 제거, 미확정 구체 단정 금지, 공간 채우기용 막연 문구 제거, 비전문가, 구어체 어휘 교정, fire 번역투('발화'→트리거) 금지, 대조 강조 구문('A가 아니라 B다') 금지, 계층 코드 라벨(L0~L5→N단계) 금지, 추상명사 의인화('근거가 수렴한다') 금지, 핵심 문장 프레임 조어('문제는 ~는 점이다', '답하는 질문은 N가지다') 금지, 핵심 문장 'X는 Y다' 복사 구조 대신 명사구 선택, 핵심 문장은 한 절만, 뒤에 줄바꿈 후 부연, 사람 동사 직역(retired→'은퇴') 금지, '더 + 동사' 비문('더 잡는다'→'더 많이 잡는다') 금지, 문서 주어 의인화('이 문서는 ~에 답한다') 금지, 요약은 볼드 훅 문장+짧은 뒷받침, 문제제기 절 필수, 단계 체계는 정의 표 선행, 문단 3문장 이내 분할, 볼드 판정 리드로 키프레임 확보, 한글 어절, 문장 경계 줄바꿈. 사용자가 "AI 말투 고쳐 / 말투 교정 / 번역체 제거 / 어투 점검 / 톤 다듬어 / 가독성 개선 / 이 문장 AI스럽다 / 제목 명사구로" 같은 요청을 하거나, 문서, 덱, 인포그래픽, 보고서를 작성, 마무리하는 모든 단계에서 트리거. 모든 한국어 문서 산출물에 상시 적용.
---

# tone-fix: AI 말투, 번역체, 가독성 교정 3단계

한국어 문서, 인포그래픽, 강의자료, 슬라이드, 보고서에서 **AI 말투와 번역체를 걷어내고, 구조를 세우고, 훑어 읽히게 만드는** 스킬. 모든 문서 산출물에 상시 적용한다. 세 단계를 순서대로 거치고, 마지막에 1단계를 다시 돌려 0건을 확인해야 마무리다.

## 핵심 원칙

**제목, 부제, 표 헤더는 명사구로 대상을 명시하고, 문단 리드는 한 절짜리 핵심 문장으로 결론을 명시한다.**

제목만 읽어도 무엇에 관한 절인지, 볼드만 읽어도 그래서 무엇이 어떻다는 것인지 파악되어야 한다. 리드 뒤에는 줄을 바꾸고 부연을 둔다. 본문은 중요성을 말로 주장하지 말고 비교축, 예시, 사실로 독자가 판단하게 한다.

## 3단계 파이프라인

| 단계 | 성격 | 대상 | 도구 | 통과 기준 |
|---|---|---|---|---|
| 1단계 패턴 스캔 | 결정론적 | 어휘, 패턴(§1~§8.14) | `scripts/check_lexical.py` | `hits: 0` |
| 2a단계 문장, 어휘 | 맥락 판단 | 문장 안의 어색한 표현 | 규칙표 §8~§8.14 읽고 재작성 | 판별법 통과 |
| 2b단계 문단, 구조 | 맥락 판단 | 요약, 문제제기, 단계 정의, 절 흐름(§10) | 규칙표 §10 읽고 재구성 | 판별법 통과 |
| 3단계 가독성 | 결정론적 + 판단 | 키프레임, 문단 길이, 강조, 리드 뒤 줄바꿈(§9, §11) | `scripts/check_md_layout.py`, `scripts/check_br.py` | `violations: 0` |

### 1단계 패턴 스캔: 결정론적

```bash
python3 scripts/check_lexical.py <file>...             # 평어체 문서
python3 scripts/check_lexical.py --honorific <file>... # 의사결정자 대상 존대 문서(§6)
```

- 1인칭, 가정형, 추측 단어, 상투 제목, 수사의문문 어미, 메타 라벨, 대시 `—` 와 가운뎃점 `·`(전면 금지), `발화`, `~가 아니라`, 계층 코드 `L0~L5`, 의인화 `근거가 수렴`, 프레임 조어 `문제는 ~는 점이다`, 사람 동사 직역 `은퇴`, `더 + 동사`, 문서 주어 의인화, 내부 참조 안내문, 관형절 제목 `결정을 바꾸는 임계치` 등 25종을 잡는다. 코드 블록 안은 건너뛴다.
- 히트마다 규칙표의 ❌→✅ 로 바꾼다. `(수동 확인)` 표시 규칙은 예외(소스 인용·업계 표준 용어·라벨 구분자)면 두되 사유를 보고에 적는다.

### 2a단계 문장, 어휘: 맥락 판단

스캔에 걸리지 않는 문장 단위 어색함을 문서를 읽으며 잡는다. 판별법이 있는 규칙은 판별법을 적용한다.

- **미확정 구체 단정**(§8.4): 방식, 시점, 동작이 소스에 실제로 있었나.
- **동의어 중복**(§8.5): 수식어 하나를 지워도 뜻이 같나.
- **의인화, 번역투**(§8.11, §8.8): 주어를 사람으로 바꿔 읽어 어색한가. 근거는 나열한다. "이 문서는 ~에 답한다" 는 문서 주어 의인화이며 질문 없이 답할 수 없으므로 `대상: ~` 명사구나 주어 없는 문장으로.
- **핵심 문장 프레임**(§8.12): 요약 훅, 볼드 리드, 절 첫 문장에서 `문제는 ~는 점이다`, `답하는 질문은 N가지다`, `~는 세 가지다`, `따라서 ~` 틀을 벗기고 주장만 남긴다.
- **핵심 문장 형태**(§8.12 형태 선택): 핵심이 구조, 개수, 대상이면 명사구로 끝내고 부연은 다음 줄에("**워크플로우 6단계.**" ↵ "모델 의존은 4단계에만"). 동작, 사건이면 실제 동사 문장. `X는 Y이다`, `명사+다` 복사 구조와 `~가 명확하다, 크다` 판정은 검출기가 `advisories:` 로 표시하므로 명사구로 바꾸거나 유지 사유를 보고에 적는다.
- **핵심 문장 한 절**(§8.12 한 절 원칙): 리드에 `-이고/-하고/-며/-지만/-어서/-므로` 로 이은 둘째 절이 있으면 부연으로 내린다. "Orca 오케스트레이션은 experimental이다." 가 리드이고 "레거시 명령은 deprecated되어 효과가 없다" 는 부연이다.
- **비교, 대비 문법**(§8.14): `더` 뒤에 동작 동사를 바로 붙이지 않는다("결함을 더 잡는다" → "더 많이 잡는다"). 대비 쌍은 반의어로("고정할 부분과 갈아 끼울 부분" → "고정 범위와 변경 범위").
- **사람 동사 직역**(§8.13): retired→`은퇴`, kill→`죽이다`, graceful→`우아한`, alive→`살아 있다` 금지. deprecated, graceful shutdown 같은 기술 용어는 원문 그대로 둔다.
- **구어, 비전문 어휘**(§8.3)와 **사용자 요청문 그대로 싣기**(§8.7).
- **자명한 부연**(§8.2): "이 표의 이름을 쓴다" 처럼 배치가 이미 보여 주는 사실을 문장으로 설명하지 않는다. 지워도 잃는 정보가 없으면 삭제.
- **관형절 + 추상명사 제목**(§8): "결정을 바꾸는 임계치" 같은 번역투는 "판단 기준" 처럼 동료에게 말할 표현으로.

### 2b단계 문단, 구조: 맥락 판단

문서 전체의 뼈대를 세운다. 순서대로 확인한다.

1. **요약**(§10.1): 항목 3~5개, 각각 볼드 훅 문장(60자 이내) + 뒷받침 1~2문장. 출처, 명령어, 수치 나열은 본문으로.
2. **문제제기 절**(§10.2): 본문 첫 절이 현재 상태 → 문제 → 이 문서가 답하는 질문을 서술하는가. 없으면 신설한다. 절 이름은 개요, 배경, 문제 무엇이든 된다.
3. **단계 체계 정의**(§10.3): `N단계` 를 쓰기 전에 `단계 | 이름 | 하는 일 | 산출물` 표가 있는가. 행동 지시 항목은 대상, 동사, 산출물을 갖는가.
4. **절 첫 문장 = 결론**(§10.4): 각 절 첫 문장만 이어 읽어 논지가 되는가.

### 3단계 가독성: 키프레임, 문단, 줄바꿈

```bash
python3 scripts/check_md_layout.py <file.md>...    # MD: §8.12 리드, §10.1, §10.2, §10.3, §11.1, §11.2, §11.3, §11.5
python3 scripts/check_br.py <file.html>...         # HTML: §9.1 문장 경계 <br>
```

- **키프레임**(§11.1): 화면 한 장(약 900자)마다 소제목, 볼드 판정 문장, 표, 목록 중 하나. 볼드 리드가 `**Orca (onorca.dev)**` 같은 라벨이면 판정 문장이나 내용이 있는 명사구로, `**문제는 ~는 점이다**` 같은 틀이면 주장만 남긴다(§8.12).
- **문단 분할**(§11.2): 문단, 목록 항목은 3문장, 260자 이내. MD 는 빈 줄로, HTML 고정폭 블록은 `<br>` 로, 명사구 꼬리는 `, ` 로.
- **강조 배분**(§11.3): 문단당 볼드 1개, 최대 2개.
- **리드 뒤 줄바꿈**(§11.5): 순서는 핵심 문장 → 줄바꿈 → 부연. MD 는 볼드 리드 단독 줄 + 빈 줄 + 부연 문단(목록 항목은 들여쓴 문단). `**라벨**: 설명` 형식만 같은 줄 허용.
- HTML 은 §9.1 을 **본문을 쓰는 시점에** 적용한다(둘째 문장을 쓰는 순간 앞 마침표 뒤 `<br>`).

### 마무리: 1단계 재실행과 보고

2, 3단계 재작성이 새 위반을 만들 수 있으므로 1단계 스캔을 다시 돌린다. 작업 보고에는 다음을 기록한다.

```
1단계 check_lexical.py: hits 0 (수동 확인 예외: §8.10 "L1 알림" 업계 용어 유지)
2a단계: §8.11 의인화 2건, §8.12 리드 프레임 3건, 복사 구조 9건 명사구화, 두 절 리드 4건 분리, §8.13 '은퇴' 1건, §8.14 '더 잡는다' 1건, §8.4 단정 1건 교정
2b단계: 요약 3항목 재작성, 배경 절 신설, 6단계 정의 표 추가
3단계 §11.5 리드 뒤 줄바꿈 38곳 적용. check_md_layout.py: advisories 1 (L84 "hook은 차단이다" 대구 구조라 유지) / violations 0 / check_br.py: violations 0
```

스크립트 0건은 필요조건이지 충분조건이 아니다. 2a, 2b 는 판별법으로 판단한 결과를 보고에 적는다. `(수동 확인)`, `advisories:` 항목은 고치거나, 유지한 건마다 사유를 적는다.

## 전체 규칙

상세 규칙·❌→✅ 대조표·판별법·실측 피드백은 [`references/tone-rules.md`](references/tone-rules.md) 한 파일에 있다.

| 단계 | 절 |
|---|---|
| 1, 2a | §1 어법, §2 1인칭, §3 가정형 명사화, §4 추측 단어, §5 친근, 메타, §6 존대 통일, §7 주어 |
| 1, 2a | §8 제목 상투, §8.1 빈 프레이밍, §8.2 메타 누출, 채우기, §8.3 수량 꼬리표, 구어 어휘, §8.4 미확정 단정, §8.5 동의어 중복 |
| 1, 2a | §8.6 대시 전면 금지, §8.7 사용자 구어 문장, §8.8 fire 번역투, §8.9 대조 강조 구문, §8.10 계층 코드 라벨, §8.11 추상명사, 문서 주어 의인화, §8.12 핵심 문장 프레임, 형태, 한 절, §8.13 사람 동사 직역, §8.14 비교, 대비 문법, §8.15 가운뎃점 금지 |
| 2b | §10 문단, 구조: 요약, 문제제기, 단계 정의, 절 첫 문장 |
| 3 | §9 어절 줄바꿈, §9.1 문장 경계 `<br>`, §11 키프레임, 문단 분할, 강조 배분, 목록, 표, 리드 뒤 줄바꿈, 점검 스크립트 |

스크립트: [`scripts/check_lexical.py`](scripts/check_lexical.py), [`scripts/check_md_layout.py`](scripts/check_md_layout.py), [`scripts/check_br.py`](scripts/check_br.py)

> `references/tone-rules.md` 가 어투, 구조 규칙의 단일 소스다. 규칙을 고칠 때는 그 파일과 `scripts/` 의 규칙 표를 함께 고친다.
